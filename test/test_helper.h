#include <cstring>

void printSucces(const char* testName, bool success) {
    if (success) {
        printf("OK: %s\n", testName);
    } else {
        
    }
}

bool areEqual(const char *str1, const char *str2) {
    
}
